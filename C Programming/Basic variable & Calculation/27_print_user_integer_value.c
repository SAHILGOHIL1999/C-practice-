#include <stdio.h>

int main ()
{
    //  Accept an Integer Value from the User and Print It
    //  scanf no use jayare user ni value print karavsms thashe
    //  & (n) = n ek address che jema value store thay 6e and tena through value forward kare 6e.
    int val;
    
    printf ("Enter integer value = ");
    scanf ("%d", &val);
    printf ("value is %d\n", val);

    return 0;
}

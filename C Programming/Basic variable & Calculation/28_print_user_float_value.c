#include <stdio.h>

int main()
{
    // Accept a Float Value from the User and Print It
    float val;
    printf("Enter float value = ");
    scanf("%f", &val);
    printf("Value is %f\n", val);
    return 0;
}
#include <stdio.h>

int main()
{
    int a, b, sum;
    printf("\nValue of A: ");
    scanf("%d", &a);
    printf("Value of B: ");
    scanf("%d", &b);
    sum = a + b;
    printf("Sum of A and B: %d", sum);
    return 0;
}
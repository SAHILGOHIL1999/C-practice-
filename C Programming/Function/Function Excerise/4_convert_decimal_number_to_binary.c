#include <stdio.h>

//  Write C Program to convert decimal number to binary using function.

void covertBinary(int);

int main()
{
    int val = 0;
    printf("Enter a value: ");
    scanf("%d",&val);

    covertBinary(val);

    return 0;
}

void covertBinary(int a)
{
    int multi = 1, binary = 0, rem = 0, step = 1;

    printf("\nStep-by-step conversion:\n");

    while(b > 0)
    {
        remainder
    }

}

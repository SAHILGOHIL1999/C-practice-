// Accept a string or character value and print it.

"use strict"

const ps = require("prompt-sync");
const prompt = ps();

let chval = prompt("Enter a string : ");
console.log("Value is " + chval + ".");

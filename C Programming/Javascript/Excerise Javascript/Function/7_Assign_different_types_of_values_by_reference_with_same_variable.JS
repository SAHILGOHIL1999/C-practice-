/* Assign different types of values by reference with same variable in functions and
print it. */

function printValue(val){
    console.log(`The value is ${val}`);
}

let a;

a = true;
printValue(a);
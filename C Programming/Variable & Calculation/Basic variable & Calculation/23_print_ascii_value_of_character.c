#include <stdio.h>

int main()
{
    //  Print the ASCII Value of a Character
    //  %d prints the integer (ASCII) value of the character.
    char ch = 'A';
    printf ("ASCII Value Of %c is %d\n",ch, ch);

    return 0;
}
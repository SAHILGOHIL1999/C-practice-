#include <stdio.h>

int main()
{
    // Print the ASCII Character for an Integer
    // %c converts the integer to its corresponding ASCII character.
    int val = 65;
    printf("ASCII character for %d is %c\n", val, val);

    return 0;
}
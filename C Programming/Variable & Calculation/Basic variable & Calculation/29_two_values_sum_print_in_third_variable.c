#include <stdio.h>

int main()
{
    // Accept Two Values, Store Their Sum in a Third Variable, and Print the Sum
    int a = 50, b = 40, sum;
    sum = a + b;
    printf ("value is %d and %d\n",a, b, sum);
    return 0;

}
#include <stdio.h>

int main()
{
    int a, b, multi;
    printf("Value of A: ");
    scanf("%d", &a);
    printf("Value of B: ");
    scanf("%d", &b);
    multi = a * b;
    printf("multiplication of A and B: %d", multi);
    return 0;
}